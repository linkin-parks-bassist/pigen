module fifo_example
	(
		input logic clk,
		input logic reset,
		input logic [7:0] data
	);

	wire [7:0] input_data;
	fifo[7:0][4] queue;

	assign input_data = data;

	always_ff @(posedge clk)
	begin
		queue <= input_data;
	end

endmodule
