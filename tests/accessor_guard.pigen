module accessor_guard
	(
		input logic clk,
		input logic reset
	);

	buf [7:0] source;
	buf [7:0] destination;

	always_ff @(posedge clk)
	begin
		if (valid ( source ))
		begin
			destination <= source;
		end
	end

endmodule
