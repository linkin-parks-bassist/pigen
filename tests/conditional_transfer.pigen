module conditional_transfer
	(
		input logic clk,
		input logic reset,
		input logic enable
	);

	buf [7:0] source;
	buf [7:0] destination;
	reg [0:0] did_transfer;

	always_ff @(posedge clk)
	begin
		if (destination <= source)
			did_transfer <= 1'b1;
	end

endmodule
