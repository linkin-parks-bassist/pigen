---
status: "unverified"
created_at: "2026-09-14T20:35:24+10:00"
scope: "local"
source: "David's current-only knowledge instruction 2026-09-14; audited current contracts and inspected owner interfaces; Codex /root"
updated_at: "2026-09-14T20:48:47+10:00"
---

The production executable accepts top-level fixed-width fabrics, e.g. `fabric command_network #(parameter integer PAYLOAD_W = 32) begin boot.tx.command > controller.rx; cpu.tx.command -> controller.rx.cpu; debugger.tx.command --> controller.rx.debugger; endfabric`. It emits flattened ready/valid boundaries, route constants and recognized-source constants. A direct exclusive link has its own two-entry endpoint queue; routed links use buffered three-port routers.

This remains current executable behavior under quarantine, while the specified inline fabric is module-owned, resolves two-component instance.port identities and checks types per connection. Remove the prototype parser and lowering at the complete fabric production cutover; never attach a partial replacement or maintain a second language. Topology, routing, endpoint/port/router/link labels, deterministic layout and diagram naming have their own current owners.
