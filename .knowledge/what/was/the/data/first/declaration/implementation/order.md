---
status: "unverified"
created_at: "2026-09-14T20:38:51+10:00"
scope: "local"
source: "Full 2026-08-27 declaration design/seven-task plan; docs fabric source/SVG; notes invariants; retirement audit Codex /root 2026-09-14"
---

Ari's approved 2026-08-27 seven-task cutover was: (1) remove Pigen byte constructor/API/family branches and replace neutral fixtures with bit[8], reserving ordinary SV byte; (2) centralize data-type-owned omission matrix and semantic positive count normalization; (3) replace split static/elastic syntax with one data-first declaration/declarator and complete checkpoint; (4) one owner-driven declaration resolver combining resolved type, written descriptor or omitted policy, argument and individual shapes/symbols; (5) adversarial SV compatibility, rollback and smallest-span diagnostic matrices; (6) update public contract/plan/editorial laws; (7) fresh focused and clean make verify, structural removal searches and code review, reproduce defects in smallest owning test and rerun changed gates/full suite. Original document outputs are replaced by semantic owner maintenance after David's retirement instruction. Historical Superpowers skill invocation prose is worker scaffolding, not a required new compiler path.

File responsibilities: data_type.h/c owns byte removal and omission; semantic.h/c owns normalization; type_resolve.c calls it; syntax.h/c plus syntax_test.c own unified topology/checkpoint/opacity; resolve.c and resolve_test.c own declarations and spans; expression_resolve/use tests prove retained generic laws; Makefile maintains build edges. Declaring children resolves shape, introduces exact name token symbol, diagnoses duplicate there and calls unified pigen_signal_add, with no parallel static loop. Scope excludes production/main, prototype text adapters, boundary specialization, initializers/aggregates/unpacked slices and pipeline declarations (their implicit buf policy belongs to later migration). Existing production fixtures remain quarantined, never a second specified dialect.

Positive matrix covers all seven concrete source transfers, unqualified int/uint/bit/Pigen aliases abstract, explicit input constraints, static bit/logic output, FIFO depth8 independent from shape2, multiple internal/ANSI names in order, count/range distinction. Failure matrix covers omitted internal/output int realization, dynamic inout, missing/empty/repeated FIFO argument, non-FIFO brackets, runtime/zero/negative depth, missing declarator/invalid continuation/initializer/duplicate/shape. Type errors point to structural type; transfer/direction to occurrence; argument to exact expression; shape to dimension; duplicate to name; decisive trailing form to first unsupported token. Ordinary wire/reg/logic/bit/ranges/ANSI continuation stay same static meaning; unsupported byte/interfaces/modports/aggregates/initializers stay opaque with exact source coverage and no arena leakage. Structural removal checks forbid STATIC_SIGNAL nodes/helpers, concrete primitive enumeration in general declaration resolution, byte constructor/APIs and transfer-first shared-frontend fixtures, but do not apply the prototype spelling search to production fixtures. Fresh final report names integration boundary, commit scope/checks and next elastic RTL task, not earlier green results.
