---
status: "unverified"
created_at: "2026-09-14T20:37:11+10:00"
scope: "local"
source: "Full 2026-08-25 data-type policy design and three-task plan; later intrinsic/declaration decisions; retirement audit 2026-09-14"
---

Ari and David's 2026-08-25 semantic-only cutover established opaque data-type family identities, structural intrinsic widths, alias-transparent numerical interpretation, conversion records and owner-resolved operations. It did not change target source spelling, syntax/main, RTL/emission, fixed-point/overflow support or production behavior. Its three tasks were canonical width maxima; primitive families/conversions; operation resolution and an explicit temporary identity-only expression boundary.

The original policy proposed two-state raw Pigen byte, same-family max-width arithmetic, optional expected-result widening, low-n-bit equal-width multiplication, unsigned shift counts preserving left width, and cast-required signed/unsigned mixing. These are historical rejected policies: later intrinsic arithmetic removes expected-result inputs and preserves mathematical ranges with legal lossless mixed promotion; later declaration cutover deletes Pigen byte while reserving ordinary signed SV byte. Never resurrect original expected-result resolver signatures, byte constructor, mixed-family rejection or identity-only expression gate. The future GF(2) addition-as-XOR idea, fixed-point scaling and overflow(expression) remain unspecified ideas with no scaffolding.

Still-valid structure: pigen_conversion stores kind, source_data_type, target_data_type; kinds include invalid, identity, integer resize/reinterpret, vector-to-integer, integer-to-vector (later promotion vocabulary is owner-defined). A single explicit vector/integer conversion may resize as well as reinterpret; full source/target records carry the complete boundary without a second width conversion. Unary/binary/conditional resolutions carry each operand conversion plus an effective operation; effective operand IDs must equal conversion targets. Build complete output records only on success; invalid IDs, zero/non-integral widths, unsupported pairs and null output fail closed without altering the caller record. Widths are structural constants, aliases follow stored targets, packed integer projections become raw packed values.

Task gates covered canonical/different-width/family identity, state/interpretation/width, aliases, invalid widths, identity/resize/reinterpret/vector conversions including unequal widths, source/target records and null outputs; width maxima's exact tests have their own leaf. Policy tests inspected every effective type/conversion, boolean results, operator-family groups, conditionals and unchanged exact-identity SV behavior. The interim walker accepted only identity resolutions until explicit conversion-node integration replaced it; this was a quarantine boundary, not fallback. Focused semantic/predicate/expression-resolve/expression-use/resolve checks and make verify, including production signed-widen behavior, verified no production change. Primitive changes stay in data_type owner and actual boundaries/tests; general walkers/features/RTL/emission do not enumerate primitive constructors. Canonical lookup/packed layout/operation-specific policy belong to small private tables/functions, never all-purpose registries or speculative metadata. Current owner records and APIs must be read from code and current leaves, not historical skeletons.
