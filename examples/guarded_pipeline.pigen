module guarded_pipeline
	(
		input logic clk,
		input logic reset,
		input logic enable,

		input  buf [7:0] in_packet,
		output buf [7:0] out_packet
	);

	always_ff @(posedge clk)
	begin
		if (enable)
		begin
			out_packet <= in_packet;
		end
	end

endmodule
